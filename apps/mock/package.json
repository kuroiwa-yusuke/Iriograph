{
  "name": "@iriograph/mock",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vue-tsc -p tsconfig.json --noEmit && vite build",
    "typecheck": "vue-tsc -p tsconfig.json --noEmit --pretty false",
    "test": "vitest run"
  },
  "dependencies": {
    "@iriograph/core": "0.8.0",
    "@iriograph/layout-elk": "0.8.0",
    "@iriograph/semantic-access": "0.8.0",
    "@iriograph/vue-editor": "0.8.0",
    "vue": "^3.5.16"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.1.3",
    "vue-tsc": "^3.0.4"
  }
}
