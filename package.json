{
  "name": "iriograph-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kuroiwa-yusuke/Iriograph.git"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:packages": "npm run build --workspace @iriograph/core && npm run build --workspace @iriograph/rdf-io && npm run build --workspace @iriograph/profile-resolver && npm run build --workspace @iriograph/semantic-access && npm run build --workspace @iriograph/layout-elk && npm run build --workspace @iriograph/profile-kit && npm run build --workspace @iriograph/presentation-tools && npm run build --workspace @iriograph/host-conformance && npm run build --workspace @iriograph/icons-aws && npm run build --workspace @iriograph/agent-bridge && npm run build --workspace @iriograph/vue-editor",
    "predev": "npm run build:packages",
    "dev": "npm run dev --workspace @iriograph/mock",
    "build": "npm run build:packages && npm run build --workspace @iriograph/mock",
    "pretest": "npm run build:packages",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "verify": "npm run verify:docs && npm run verify:package-versions && npm run test && npm run typecheck && npm run build && npm run verify:package-consumer",
    "verify:docs": "node scripts/verify-docs.mjs",
    "verify:e2e": "playwright test",
    "verify:browser-performance": "playwright test --config playwright.browser-performance.config.ts",
    "verify:performance": "playwright test --config playwright.performance.config.ts",
    "verify:package-consumer": "npm run build:packages && node scripts/verify-package-consumer.mjs",
    "verify:package-versions": "node scripts/verify-package-versions.mjs && node --test scripts/publish-packages.test.mjs",
    "publish:packages": "node scripts/publish-packages.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@types/node": "^24.3.0",
    "typescript": "^5.8.3",
    "vitest": "^4.1.8"
  }
}
